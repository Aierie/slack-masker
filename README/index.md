# slack-masker
Prototype for adding html files to a git repo based on user input from slack

# What it does
1. Open a modal using shortcuts
   ![Open a modal by using shortcuts](shortcut%20trigger.png)
2. 